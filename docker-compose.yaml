services:
  money-utils:
    build:
      context: .
      args:
        - SOURCE_COMMIT
      dockerfile: Dockerfile
    ports:
      - 5002:5002
    volumes:
      - .env:/app/.env:ro
      - ./volumes:/app/volumes
    restart: unless-stopped

